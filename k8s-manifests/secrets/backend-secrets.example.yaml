# backend-secrets.yaml — TEMPLATE ONLY
# DO NOT commit this file with real values!
# Instead, create the secret via kubectl:
#
#   kubectl create secret generic backend-secrets \
#     --from-literal=PG_PASSWORD=your_pg_password \
#     --from-literal=GIT_TOKEN=your_github_pat \
#     --from-literal=ARGOCD_TOKEN=your_argocd_token \
#     -n default

apiVersion: v1
kind: Secret
metadata:
  name: backend-secrets
  namespace: default
type: Opaque
stringData:
  PG_PASSWORD: "CHANGE_ME"
  GIT_TOKEN: "CHANGE_ME"
  ARGOCD_TOKEN: "CHANGE_ME"
