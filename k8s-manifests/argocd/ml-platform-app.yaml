apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: ml-platform
  namespace: argocd
spec:
  project: default
  source:
    # CHANGE THIS to your actual GitHub repo URL
    repoURL: https://github.com/YOUR_USERNAME/foundational-model-hosting-platform.git
    targetRevision: main
    path: k8s-manifests/manifests
    directory:
      recurse: true
  destination:
    server: https://kubernetes.default.svc
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
